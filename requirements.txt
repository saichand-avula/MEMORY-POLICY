# Core ML / training
torch>=2.2.0
transformers>=4.45.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.11.0
accelerate>=0.33.0

# Optional: 4-bit quantisation (NVIDIA GPU only)
# bitsandbytes>=0.43.0

# Optional: faster tokenization
tokenizers>=0.19.0

# Utilities
sentencepiece>=0.2.0    # needed by some tokenizers
protobuf>=3.20.0


